Features serve two purposes:

- maintainability: make the main VisibilityToggleController (previously OverlayController) smaller, modular and readable
- on demand loading: make every disclosure / overlay (c.q. every visibility toggle) as performant as possible

