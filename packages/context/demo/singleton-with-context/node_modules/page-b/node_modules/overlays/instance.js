import { OverlaysManager } from './index.js';

import { singletonManager } from 'singleton-manager';

export const overlays = singletonManager.get('overlays::instance.js::2.x') || new OverlaysManager();
