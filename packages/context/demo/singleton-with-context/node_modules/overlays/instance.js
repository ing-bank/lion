import { OverlaysManager, overlaysId, overlaysRange } from './index.js';

import { singletonManager } from 'singleton-manager';

export const overlays = singletonManager.get(overlaysRange) || new OverlaysManager();
