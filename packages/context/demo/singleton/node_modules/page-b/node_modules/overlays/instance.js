import { OverlaysManager } from './index.js';

import { singletonManager } from 'singleton-manager';

export const overlays = singletonManager.get('overlays::index.js::2.x') || new OverlaysManager();
